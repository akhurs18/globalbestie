{
  "name": "global-bestie-commerce",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "node scripts/check-static-app.mjs && node scripts/check-rendered-app.mjs --allow-skip",
    "check:render": "node scripts/check-rendered-app.mjs",
    "marketing:daily": "node scripts/run-daily-marketing-agent.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.48.0"
  }
}
